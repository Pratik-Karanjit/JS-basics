let scattered = ["Hel lo Wo rld, 1, Ja va script is awe some"];

// Remove spaces and join the characters
let joinedWithoutSpaces = scattered[0].split(' ').join('');

console.log(joinedWithoutSpaces);
