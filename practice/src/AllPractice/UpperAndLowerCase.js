//The syntax is the string that needs to be converted into upper case then .toUpperCase()

let myString = "hello";

let converter = (input) => {
  return input.toUpperCase();
};

console.log(converter(myString));
