console.log(Math.random()); //Gives random number between 0 and 1

let floatValue = 7.1;
console.log(Math.floor(floatValue)); //Ignores the decimal point and anything after it.
console.log(Math.ceil(floatValue)); //Rounds off the decimal point.
