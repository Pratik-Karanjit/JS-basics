//A first-order function is a function that doesn't take another function as an argument or return a function as its result.

function add(a, b) {
  return a + b;
}

let result = add(2, 3);
console.log(result);
