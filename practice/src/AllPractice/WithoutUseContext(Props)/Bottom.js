import React from 'react'

const Bottom = (props) => {
  return (
    <div>
        {/* Displaying the name from props.name */}
        This is child where I am receiving {props.name}
    </div>
  )
}

export default Bottom